apiVersion: v1
kind: Service
metadata:
  annotations: {}
  name: ml-training-service
  namespace: apostapro-ml
spec:
  ports:
  - name: http
    port: 8080
    targetPort: 8080
  selector:
    app: ml-training
  type: ClusterIP
